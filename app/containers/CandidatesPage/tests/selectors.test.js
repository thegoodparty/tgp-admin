// import { selectCandidatesPageDomain } from '../selectors';

describe('selectCandidatesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
